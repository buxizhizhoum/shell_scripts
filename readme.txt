This is the shell script folder

modify_track_point.sh is used to modify the move speed of Thindpad under Ubuntu system.

Usage:

cd to the directory where the file is located in, and run it by: sh modify_track_point.sh
